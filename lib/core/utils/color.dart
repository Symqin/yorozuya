import 'package:flutter/material.dart';

class AppColors {
  // Primary: Sage Green (Tenang, Alam, Terpercaya)
  static const Color primary = Color(0xFF5D8C76);

  // Secondary: Wood Beige (Aksen pendukung, Elegan)
  static const Color secondary = Color(0xFFD7CCC8);

  // Accent: Persimmon Orange (Untuk tombol "Beli" atau Diskon agar mencolok)
  static const Color accent = Color(0xFFFF7043);

  // Background: Warm Off-white (Tidak menyakitkan mata seperti putih murni)
  static const Color background = Color(0xFFF5F5F0);

  // Text: Blue Grey (Lebih lembut dari hitam pekat)
  static const Color textDark = Color(0xFF37474F);
  static const Color textLight = Color(
    0xFFFAFAFA,
  ); // Untuk teks di atas tombol hijau/oranye
}
